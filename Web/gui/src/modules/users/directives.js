'use strict';

define([
        'angular',
        ], function(angular) {

	var moduleName = 'kasparGUI.users.directives';
	var dependancies = [];

	var module = angular.module(moduleName, dependancies);
	
	return moduleName;
});
