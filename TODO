Interactions:
	Pretty much everything

Overall:
    Warning/Error handling
    UI Cleanup
    Session table with current robot/interaction
    
New:
	'Debug' Screen
		setPosition(all, default)
		for each servo:
			getPosition() == Communication Good
			setPosition(min/max, 25%) == Visual check, physical range check
				On err, setPosition(default, 100%)
					if err, break
